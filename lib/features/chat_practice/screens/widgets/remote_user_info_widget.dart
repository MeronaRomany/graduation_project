import 'package:flutter/material.dart';

class RemoteUserInfoWidget extends StatelessWidget {
  const RemoteUserInfoWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}