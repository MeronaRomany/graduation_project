import 'package:flutter/material.dart';

class ColorsManager {
  // Brand palette
  static const Color primary = Color(0xFF04E9C7);
  static const Color accent = Color(0xFF04E9C7);

  // Light mode surfaces
  static const Color lightSurface = Colors.white;
  static const Color lightBackground = Color(0xFF04E9C7);
  static const Color lightAppBarForeground = Colors.black;

  // Dark mode surfaces
  static const Color darkSurface = Color(0xFF121212);
  static const Color darkBackground = Color(0xFF0E0E0E);
  static const Color darkAppBarForeground = Colors.white;
}
